  코드 (수정/신규)

  src/services/corpus_loader.py   ← 수정
  src/services/json_parser.py     ← 수정
  src/services/pid_translations.py ← 신규

  데이터 (둘 중 하나)

  - 벡터DB 전달: data/vector_db/ 폴더 전체 (재빌드 후) — 용량 크지만 동료가 바로 실행 가능
  - 코퍼스 전달: data/corpus/ 폴더 전체 — 동료가 직접 corpus_loader.py 실행해서 빌드

  src/services/corpus_loader.py, src/services/json_parser.py, src/services/pid_translations.py